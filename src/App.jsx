import React from 'react';
import { hot } from 'react-hot-loader/root';
import './app.css';

const App = () => {
  return (
    <div className="app">
      <h1>React Starter</h1>
    </div>
  );
};

export default hot(App);
